role == 1 (Admin/Executive)
role == 3 (loan officers (user->office_id))
role == 4 (Branch Manager (user->office_id))

role == 12 (DM Manager (user->office->district_id))
role == 6 (Provincial Manager(user->province_id) )
